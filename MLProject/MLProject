name: SML_Trio-Anggoro_CI

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      penalty: {type: str, default: "l2"}
      C: {type: float, default: 0.1}
      solver: {type: str, default: "liblinear"}
      dataset: {type: string, default: "train_pca.csv"}
    command: "python modelling.py {penalty} {C} {solver} {dataset}"
